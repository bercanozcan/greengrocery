<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script src="https://kit.fontawesome.com/2314c4ea75.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>{{title}}</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            border: none;
            text-decoration: none;
            list-style: none;
        }

        *:focus {
            outline: 0;
        }

        button:focus {
            outline: 0;
        }

        html,
        body {
            height: 100%;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans",
                "Helvetica Neue", sans-serif;
        }

        h1 {
            font-size: 24px;
        }

        h2 {
            font-size: 20px;
        }

        h3 {
            font-size: 18px;
        }

        h4 {
            font-size: 16px;
        }

        .card {
            border: 1px solid #dedede;
            cursor: pointer;
            margin-bottom: 30px;
        }

        .card .qty {
            position: absolute;
            display: flex;
            align-items: center;
            background-color: #fff;
            width: 100%;
            height: 100%;
            justify-content: center;
            flex-direction: column;
            z-index: -1;
        }

        .card .qty input {
            width: 50px;
            text-align: center;
            font-size: 32px;
        }

        .card .qty .crease {
            padding: 30px;
        }

        button.qty-close {
            position: absolute;
            top: 0px;
            right: 1px;
            padding: 20px 25px;
        }

        .card .add-cart {
            position: absolute;
            right: 0px;
        }

        .card .add-cart button.btn-add-cart {
            padding: 20px 25px;
        }

        .card img {
            border-radius: 20px;
        }

        .card-body {
            height: 90px;
            padding: 15px;
        }

        .card-body h4 {
            margin-bottom: 0px;
            letter-spacing: .4px;
            line-height: 1.4;
        }

        .card-body span {
            color: #afafaf;
            font-size: 14px;
        }
    </style>
</head>

<body>

    <div class="container">
        {{> header }}
        {{{body}}}
        {{> footer }}
    </div>